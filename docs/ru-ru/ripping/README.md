# Риппинг

_**<---> Более подробное описание в этом разделе будет добавлено позже :P <--->**_

## .cue, .log и .accurip файлы

Файлы `.cue`, `.log` и `.accurip` именуются по шаблону `CD<Номер диска>`: `CD1.cue`, `CD1.log`, `CD1.accurip`, `CD2.cue` и так далее.  
Если релиз состоит всего из одного диска, то префикс с номером опускается: `CD.cue`, `CD.log`, `CD.accurip`.

Я генерирую `.accurip` файлы с помощью программы [CUETools](https://cue.tools/wiki/CUETools) для повторной верификации сторонних рипов на основе существующих `.log` файлов.
